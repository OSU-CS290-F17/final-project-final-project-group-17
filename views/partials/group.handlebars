<div class="group-container" id="group-{{_id}}" >
	<span class="group-container-header" id="group-{{_id}}-header">
		<h2 class="group-header-title">Group {{_id}}</h2>
	</span>
	{{#each tasks}}
		 {{> task}}
	{{/each}}
</div>
